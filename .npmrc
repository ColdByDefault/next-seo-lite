# No registry override needed here.
# Publish scripts in package.json use explicit --registry flags.
# Users installing from npm need no .npmrc at all.
# Users installing from GitHub Packages should add:
#   @coldbydefault:registry=https://npm.pkg.github.com
