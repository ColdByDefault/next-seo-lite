# Override the global ~/.npmrc scope so publish:npm correctly targets npmjs.org
# publish:github explicitly passes --registry https://npm.pkg.github.com
@coldbydefault:registry=https://registry.npmjs.org
